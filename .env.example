# ProtonDeck — copie pra .env e ajuste

# Chave de assinatura/criptografia da sessao (64 hex chars = 32 bytes).
# Gere com: openssl rand -hex 32
SESSION_KEY=

# Porta e bind
PORT=3030
HOST=127.0.0.1

# Production: o cookie sai como Secure (so HTTPS)
# NODE_ENV=production
